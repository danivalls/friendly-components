import React from 'react';
import { SectionContent } from './DocSectionContent.styled';

const DocSectionContent = ({ children }) => {
  return <SectionContent>{children}</SectionContent>;
};

export default DocSectionContent;
